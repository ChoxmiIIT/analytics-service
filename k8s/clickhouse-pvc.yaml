apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clickhouse-analytics-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: cmm707-sc
  resources:
    requests:
      storage: 2Gi